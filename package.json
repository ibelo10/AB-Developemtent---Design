{
  "name": "ab-development-site",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "scripts": {
    "build": "cd projects/transaction-ticker && npm install && npm run build",
    "start": "cd projects/transaction-ticker && npm start"
  },
  "workspaces": [
    "projects/*",
    "netlify/functions"
  ]
}